info:
  name: Get Task By Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/tasks/slug/:slug"
  params:
    - name: slug
      value: "{{?taskSlug}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
