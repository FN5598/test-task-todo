info:
  name: Refresh Session (Frontend)
  type: http
  seq: 9

http:
  method: POST
  url: "{{CLIENT_URL}}/api/auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
