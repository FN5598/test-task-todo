info:
  name: Refresh Token
  type: http
  seq: 9

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/refresh-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
