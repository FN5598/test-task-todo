info:
  name: Create Task
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/tasks"
  body:
    type: json
    data: |-
      {
        "userId": "1b6f7954-f5ee-4a41-ad36-fea77e7c21d7",
        "title": "Some task",
        "description": "super good description",
        "status": "todo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
