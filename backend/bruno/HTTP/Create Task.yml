info:
  name: Create Task
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Some task",
        "description": "super good description",
        "status": "todo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
