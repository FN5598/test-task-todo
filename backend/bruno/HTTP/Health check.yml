info:
  name: Health check
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
