info:
  name: Update Task By Id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/api/tasks/:id"
  params:
    - name: id
      value: "{{?taskId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Even better title"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
