info:
  name: Get Task Counts
  type: http
  seq: 11

http:
  method: GET
  url: "{{BASE_URL}}/api/tasks/counts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
